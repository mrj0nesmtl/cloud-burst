import React from 'react'

// This component serves as the default slot for parallel routes
export default function Default(): React.ReactNode {
  // Return null or an empty fragment to render nothing
  return null
} 